<div class="mt-4">
    <div class="row">
        <div class="col-lg-3">
            <p>Something here</p>
        </div>
        <div class="col-lg-9">
            <h2>{{course.name}}</h2>
            <img class="image-detail" src="{{course.image}}" alt="{{course.name}}" >
            <p>{{course.description}}</p>
        </div>
    </div>
</div>